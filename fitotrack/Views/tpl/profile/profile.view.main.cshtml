<div class="container" data-ng-cloak>
    <div class="row">
        <div class="col-xs-3">
            <div class="list-group" id="profile-left-menu">
                <a class="list-group-item"
                   href=""
                   data-ng-repeat="item in menuItems"
                   data-ui-sref="{{item.state}}"
                   data-ui-sref-active="active">{{item.name}}</a>
            </div>
        </div>

        <div class="col-xs-9">
            <h3>Profile</h3>
            <div data-ui-view=""></div>
        </div>
    </div>
</div>